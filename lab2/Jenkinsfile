pipeline {
    agent { docker { image 'python:3.10.13-alpine3.19' } }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
        stage('git') {
            steps {
                echo 'Git'
                git(
                    url: "https://github.com/YsmanKhamidullin/ML-OPS",
                    branch: "main",
                    changelog: true
                )
            }
        }

        stage('Install Dependencies') {
            steps {
                echo 'Installing dependencies'
                dir('./') {
                    sh 'pip install -r requirements.txt'
                }
            }
        }

        stage('ML-Pipeline') {
            steps {
                echo 'Calling pipeline.sh'
                dir('./lab1/') {
                    sh './pipeline.sh'
                }
            }
        }
    }
}
